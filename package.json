{
  "name": "json-complete",
  "version": "1.0.0",
  "description": "An encoder that can turn any standard JavaScript object or value into a form that can be serialized by JSON, and do the reverse with a decoder. This includes preserving referential integrety and deduplication of data in the encoded output.",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "repository": {
    "type": "git",
    "url": "ssh://git@stash.cierecloud.com:7999/cier/json-complete.git"
  },
  "author": "Dwight House",
  "license": "MIT",
  "devDependencies": {
    "browser-sync": "2.24.7",
    "gulp": "3.9.1"
  }
}
